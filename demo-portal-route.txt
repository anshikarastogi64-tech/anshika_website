
// Demo Portal Route - Shows sample client portal
router.get('/demo', (req, res) => {
  // Sample demo project data
  const demoProject = {
    id: 'DEMO-001',
    name: 'Luxury Villa - Noida Extension',
    clientName: 'Demo User',
    clientEmail: 'demo@example.com',
    clientPhone: '+91 9999999999',
    status: 'In Progress',
    progress: 65,
    budget: 5000000,
    spent: 3250000,
    startDate: new Date(Date.now() - 90 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    estimatedCompletion: new Date(Date.now() + 60 * 24 * 60 * 60 * 1000).toISOString().split('T')[0],
    currentPhase: 'Interior Finishing',
    timeline: [
      { phase: 'Site Preparation', status: 'Completed', date: new Date(Date.now() - 85 * 24 * 60 * 60 * 1000).toISOString().split('T')[0] },
      { phase: 'Foundation Work', status: 'Completed', date: new Date(Date.now() - 70 * 24 * 60 * 60 * 1000).toISOString().split('T')[0] },
      { phase: 'Structure', status: 'Completed', date: new Date(Date.now() - 50 * 24 * 60 * 60 * 1000).toISOString().split('T')[0] },
      { phase: 'Interior Finishing', status: 'In Progress', date: 'Ongoing' },
      { phase: 'Final Touches', status: 'Upcoming', date: 'Next Month' }
    ],
    recentUpdates: [
      { date: new Date(Date.now() - 2 * 24 * 60 * 60 * 1000).toISOString().split('T')[0], update: 'Master bedroom flooring completed', image: null },
      { date: new Date(Date.now() - 5 * 24 * 60 * 60 * 1000).toISOString().split('T')[0], update: 'Kitchen cabinets installed', image: null },
      { date: new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString().split('T')[0], update: 'Living room painting finished', image: null }
    ],
    payments: [
      { date: new Date(Date.now() - 85 * 24 * 60 * 60 * 1000).toISOString().split('T')[0], description: 'Initial Payment', amount: 1000000, status: 'Paid' },
      { date: new Date(Date.now() - 70 * 24 * 60 * 60 * 1000).toISOString().split('T')[0], description: 'Foundation Milestone', amount: 1250000, status: 'Paid' },
      { date: new Date(Date.now() - 50 * 24 * 60 * 60 * 1000).toISOString().split('T')[0], description: 'Structure Complete', amount: 1000000, status: 'Paid' },
      { date: 'Upcoming', description: 'Interior Completion', amount: 1000000, status: 'Pending' },
      { date: 'Final', description: 'Final Payment', amount: 750000, status: 'Pending' }
    ],
    cctvAvailable: true
  };

  res.send(`
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Demo Client Portal - Kelly Designers Vision</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
      color: #fff;
      min-height: 100vh;
      padding: 20px;
    }
    .demo-banner {
      background: linear-gradient(135deg, #D4AF37 0%, #FFA500 100%);
      color: #1a1a2e;
      padding: 15px 30px;
      text-align: center;
      border-radius: 15px;
      margin-bottom: 30px;
      font-weight: 600;
      box-shadow: 0 10px 40px rgba(212, 175, 55, 0.3);
    }
    .container { max-width: 1200px; margin: 0 auto; }
    .header {
      background: rgba(255,255,255,0.05);
      border-radius: 20px;
      padding: 30px;
      margin-bottom: 30px;
      backdrop-filter: blur(10px);
      border: 2px solid rgba(212, 175, 55, 0.3);
    }
    .project-name {
      font-family: 'Playfair Display', serif;
      font-size: 32px;
      color: #D4AF37;
      margin-bottom: 10px;
    }
    .project-status {
      display: inline-block;
      background: #4CAF50;
      padding: 8px 20px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: 600;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }
    .stat-card {
      background: rgba(255,255,255,0.08);
      border-radius: 15px;
      padding: 25px;
      border: 1px solid rgba(212, 175, 55, 0.2);
    }
    .stat-label {
      font-size: 14px;
      color: rgba(255,255,255,0.7);
      margin-bottom: 8px;
    }
    .stat-value {
      font-size: 28px;
      font-weight: 700;
      color: #D4AF37;
    }
    .progress-bar-container {
      background: rgba(255,255,255,0.1);
      height: 30px;
      border-radius: 15px;
      overflow: hidden;
      margin-top: 10px;
    }
    .progress-bar {
      background: linear-gradient(90deg, #D4AF37 0%, #4CAF50 100%);
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 600;
      font-size: 14px;
      transition: width 1s ease;
    }
    .section {
      background: rgba(255,255,255,0.05);
      border-radius: 20px;
      padding: 30px;
      margin-bottom: 30px;
      border: 1px solid rgba(212, 175, 55, 0.2);
    }
    .section-title {
      font-family: 'Playfair Display', serif;
      font-size: 24px;
      color: #D4AF37;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 2px solid rgba(212, 175, 55, 0.3);
    }
    .timeline-item {
      display: flex;
      align-items: center;
      padding: 15px;
      background: rgba(255,255,255,0.03);
      border-radius: 10px;
      margin-bottom: 10px;
      border-left: 4px solid #D4AF37;
    }
    .timeline-item.completed { border-left-color: #4CAF50; }
    .timeline-item.pending { border-left-color: #666; }
    .timeline-phase {
      flex: 1;
      font-weight: 600;
    }
    .timeline-status {
      padding: 5px 15px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
    }
    .timeline-status.completed { background: #4CAF50; }
    .timeline-status.progress { background: #FFA500; }
    .timeline-status.upcoming { background: #666; }
    .update-item {
      padding: 15px;
      background: rgba(255,255,255,0.03);
      border-radius: 10px;
      margin-bottom: 10px;
      border-left: 3px solid #D4AF37;
    }
    .update-date {
      font-size: 12px;
      color: #D4AF37;
      margin-bottom: 5px;
    }
    .payment-item {
      display: grid;
      grid-template-columns: 150px 1fr 150px 100px;
      gap: 20px;
      padding: 15px;
      background: rgba(255,255,255,0.03);
      border-radius: 10px;
      margin-bottom: 10px;
      align-items: center;
    }
    .payment-status {
      padding: 5px 15px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
      text-align: center;
    }
    .payment-status.paid { background: #4CAF50; }
    .payment-status.pending { background: #FFA500; }
    .back-btn {
      display: inline-block;
      padding: 12px 30px;
      background: rgba(255,255,255,0.1);
      color: #D4AF37;
      text-decoration: none;
      border-radius: 25px;
      font-weight: 600;
      border: 2px solid #D4AF37;
      transition: all 0.3s;
      margin-top: 20px;
    }
    .back-btn:hover {
      background: #D4AF37;
      color: #1a1a2e;
    }
    @media (max-width: 768px) {
      .payment-item {
        grid-template-columns: 1fr;
        gap: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="demo-banner">
    🚀 DEMO MODE - This is a sample portal showing how you'll track your dream project in real-time
  </div>

  <div class="container">
    <div class="header">
      <h1 class="project-name">${demoProject.name}</h1>
      <span class="project-status">● ${demoProject.status}</span>
      <div style="margin-top: 20px; color: rgba(255,255,255,0.8);">
        <div>👤 ${demoProject.clientName}</div>
        <div>📧 ${demoProject.clientEmail}</div>
        <div>📞 ${demoProject.clientPhone}</div>
      </div>
    </div>

    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-label">Project Progress</div>
        <div class="stat-value">${demoProject.progress}%</div>
        <div class="progress-bar-container">
          <div class="progress-bar" style="width: ${demoProject.progress}%">${demoProject.progress}%</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Total Budget</div>
        <div class="stat-value">₹${(demoProject.budget / 100000).toFixed(1)}L</div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Amount Spent</div>
        <div class="stat-value">₹${(demoProject.spent / 100000).toFixed(1)}L</div>
      </div>
      <div class="stat-card">
        <div class="stat-label">Current Phase</div>
        <div class="stat-value" style="font-size: 20px;">${demoProject.currentPhase}</div>
      </div>
    </div>

    <div class="section">
      <h2 class="section-title">📅 Project Timeline</h2>
      ${demoProject.timeline.map(item => `
        <div class="timeline-item ${item.status.toLowerCase().replace(' ', '-')}">
          <div class="timeline-phase">${item.phase}</div>
          <div style="color: rgba(255,255,255,0.6); margin: 0 15px;">${item.date}</div>
          <div class="timeline-status ${item.status === 'Completed' ? 'completed' : item.status === 'In Progress' ? 'progress' : 'upcoming'}">
            ${item.status}
          </div>
        </div>
      `).join('')}
    </div>

    <div class="section">
      <h2 class="section-title">📊 Recent Updates</h2>
      ${demoProject.recentUpdates.map(item => `
        <div class="update-item">
          <div class="update-date">${item.date}</div>
          <div>${item.update}</div>
        </div>
      `).join('')}
    </div>

    <div class="section">
      <h2 class="section-title">💰 Payment Schedule</h2>
      ${demoProject.payments.map(item => `
        <div class="payment-item">
          <div style="color: rgba(255,255,255,0.7);">${item.date}</div>
          <div style="font-weight: 600;">${item.description}</div>
          <div style="color: #D4AF37; font-weight: 700; font-size: 18px;">₹${(item.amount / 100000).toFixed(1)}L</div>
          <div class="payment-status ${item.status.toLowerCase()}">${item.status}</div>
        </div>
      `).join('')}
    </div>

    <div class="section" style="text-align: center; background: linear-gradient(135deg, rgba(212, 175, 55, 0.1), rgba(255, 165, 0, 0.1)); border: 2px solid rgba(212, 175, 55, 0.3);">
      <h2 class="section-title">📹 24/7 CCTV Monitoring Available</h2>
      <p style="color: rgba(255,255,255,0.8); margin-bottom: 20px;">
        Your actual portal will include live CCTV feeds from your project site, allowing you to monitor construction progress anytime, anywhere.
      </p>
      <div style="font-size: 48px; margin: 20px 0;">🎥 📹 👁️</div>
    </div>

    <div style="text-align: center;">
      <a href="/Kelly/index.html" class="back-btn">← Back to Main Site</a>
      <p style="margin-top: 20px; color: rgba(255,255,255,0.6);">
        Ready to start your own dream project? <a href="/contact" style="color: #D4AF37;">Contact us today!</a>
      </p>
    </div>
  </div>
</body>
</html>
  `);
});
